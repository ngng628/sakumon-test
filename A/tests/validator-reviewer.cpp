# include "testlib.h"
# include <bits/stdc++.h>
using namespace std;

# include "constraints-reviewer.h" // 制約

int main(int argc, char* argv[]) {
   registerValidation(argc, argv);

   int a = inf.readInt(A_MIN, A_MAX, "A");
   inf.readSpace();
   int b = inf.readInt(A_MIN, A_MAX, "B");
   inf.readEoln();
   inf.readEof();

   return 0;
}
